import { Typography } from '@mui/material'

const Horimetro = () => {
  return (
    <div>
      <Typography variant="h4">Horímetro</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        Página de histórico de horímetros (em desenvolvimento)
      </Typography>
    </div>
  )
}

export default Horimetro
